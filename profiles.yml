default:
  outputs:
    dev:
      type: bigquery
      method: service-account
      keyfile: /Users/shaziakn/.dbt/crypto-stone-312806-bd20a817f207.json # replace this with the full path to your keyfile
      project: crypto-stone-312806 # Replace this with your project id
      schema: cms_medicare_dev # Replace this with dbt_your_name, e.g. dbt_bob
      threads: 1
      timeout_seconds: 300
      location: US
      priority: interactive
  target: dev

medicare_pipeline: # this needs to match the profile: in your dbt_project.yml file
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      keyfile: /Users/shaziakn/.dbt/crypto-stone-312806-bd20a817f207.json # replace this with the full path to your keyfile
      project: crypto-stone-312806 # Replace this with your project id
      schema: cms_medicare_dev # Replace this with dbt_your_name, e.g. dbt_bob
      threads: 1
      timeout_seconds: 300
      location: US
      priority: interactive
    prod:
      type: bigquery
      method: service-account
      keyfile: /Users/shaziakn/.dbt/crypto-stone-312806-bd20a817f207.json # replace this with the full path to your keyfile
      project: crypto-stone-312806 # Replace this with your project id
      schema: cms_medicare # Replace this with dbt_your_name, e.g. dbt_bob
      threads: 1
      timeout_seconds: 300
      location: US
      priority: interactive
